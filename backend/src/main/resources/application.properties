# Application Name
spring.application.name=backend

# === DATABASE CONFIGURATION ===
spring.datasource.url=jdbc:mysql://localhost:3306/transparency_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Swati@9515
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === JPA / HIBERNATE CONFIGURATION ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Logs the SQL with values bound (e.g., insert into users values ('John', 'email@example.com'))
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# === SERVER CONFIGURATION ===
server.port=8080

# === OPTIONAL: BASIC SECURITY (if Spring Security is enabled) ===
spring.security.user.name=admin
spring.security.user.password=admin123
